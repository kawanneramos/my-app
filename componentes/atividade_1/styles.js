import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
   container: {
    backgroundColor: 'red',
    padding: 10,
   },
   texto: {
    color: '#fff',
    fontSize: 20,
   },
});

export default styles;